# Runtime dependencies — pinned loose on purpose so a fresh clone
# pulls compatible up-to-date versions.  CI should pin-lock this.

# --- Core ---
pydantic>=2.0,<3.0
pyyaml>=6.0
httpx>=0.27
numpy>=1.24,<3.0

# --- REPL line editing on macOS ---
# macOS's system Python binds `import readline` to libedit, which
# mishandles \001/\002 prompt-width markers and corrupts backspace
# on wrapped lines. gnureadline is a drop-in GNU readline that
# Python picks up instead, making the existing REPL code work.
gnureadline>=8.1; sys_platform == "darwin"

# --- LLM client (Vertex AI via google-genai) ---
google-genai>=0.5
google-auth>=2.30

# --- Semantic search ---
faiss-cpu>=1.10                  # CPU FAISS is fine for runtime queries

# --- Data-layer hydration (HF artifact downloads) ---
huggingface_hub>=0.24            # snapshot_download for /data layer

# --- Dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
tqdm>=4.66
